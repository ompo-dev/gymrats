# api

- Caminho: `app/api`
- Finalidade: raiz do App Router com páginas, layouts e endpoints por filesystem routing.
- Endpoint base deste diretorio: `/api`

## Subpastas
- `auth/`: subdomínio de `app/api/auth`.
- `errors/`: subdomínio de `app/api/errors`.
- `exercises/`: subdomínio de `app/api/exercises`.
- `foods/`: subdomínio de `app/api/foods`.
- `gym-subscriptions/`: subdomínio de `app/api/gym-subscriptions`.
- `gyms/`: subdomínio de `app/api/gyms`.
- `memberships/`: subdomínio de `app/api/memberships`.
- `nutrition/`: subdomínio de `app/api/nutrition`.
- `payment-methods/`: subdomínio de `app/api/payment-methods`.
- `payments/`: subdomínio de `app/api/payments`.
- `students/`: subdomínio de `app/api/students`.
- `subscriptions/`: subdomínio de `app/api/subscriptions`.
- `swagger/`: subdomínio de `app/api/swagger`.
- `users/`: subdomínio de `app/api/users`.
- `webhooks/`: subdomínio de `app/api/webhooks`.
- `workouts/`: subdomínio de `app/api/workouts`.

## Arquivos
- Nenhum arquivo além do README.

## Observações
- Leitura gerada por análise estática de símbolos, chamadas e imports do diretório e vizinhança de uso.
- Para operação em produção, complementar com contratos de payload, códigos de erro, invariantes e cenários de falha.
